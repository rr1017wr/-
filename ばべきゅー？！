-- BBQ It GUI Autofarm Script (2026対応調整版)
-- Auto Buy Meat/Gear + Auto Discard など

local player = game.Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local ws = workspace

print("BBQ It GUI Script Loaded 🔥")

-- ==================== GUI作成 ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BBQItGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 460)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -230)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundColor3 = Color3.fromRGB(255, 80, 0)
Title.Text = "BBQ It Autofarm GUI"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local Scrolling = Instance.new("ScrollingFrame")
Scrolling.Size = UDim2.new(1, -20, 1, -70)
Scrolling.Position = UDim2.new(0, 10, 0, 60)
Scrolling.BackgroundTransparency = 1
Scrolling.ScrollBarThickness = 8
Scrolling.Parent = MainFrame

local UIList = Instance.new("UIListLayout")
UIList.Padding = UDim.new(0, 10)
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Parent = Scrolling

local function CreateToggle(name, default)
    local ToggleFrame = Instance.new("Frame")
    ToggleFrame.Size = UDim2.new(1, -10, 0, 55)
    ToggleFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    ToggleFrame.Parent = Scrolling
    
    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.65, 0, 1, 0)
    Label.BackgroundTransparency = 1
    Label.Text = " " .. name
    Label.TextColor3 = Color3.new(1,1,1)
    Label.TextScaled = true
    Label.Font = Enum.Font.Gotham
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = ToggleFrame
    
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.28, 0, 0.75, 0)
    Button.Position = UDim2.new(0.7, 0, 0.12, 0)
    Button.BackgroundColor3 = default and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(200, 0, 0)
    Button.Text = default and "ON" or "OFF"
    Button.TextColor3 = Color3.new(1,1,1)
    Button.TextScaled = true
    Button.Font = Enum.Font.GothamBold
    Button.Parent = ToggleFrame
    
    local enabled = default
    Button.MouseButton1Click:Connect(function()
        enabled = not enabled
        Button.BackgroundColor3 = enabled and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(200, 0, 0)
        Button.Text = enabled and "ON" or "OFF"
    end)
    
    return function() return enabled end
end

-- ==================== 設定 ====================
local AutoCook    = CreateToggle("Auto Cook", true)
local AutoPlate   = CreateToggle("Auto Plate", true)
local AutoSell    = CreateToggle("Auto Sell", true)
local AutoDiscard = CreateToggle("Auto Discard Burnt", true)
local AutoBuyMeat = CreateToggle("Auto Buy Meat", true)
local AutoBuyGear = CreateToggle("Auto Buy Gear / Totem", true)

-- 買うアイテムリスト（必要に応じて追加・変更）
local MEATS = {"BasicMeat", "Chicken", "Pork", "Steak", "PremiumMeat"}
local GEARS = {"GrowthTotem", "GrillUpgrade", "Table", "Smoker", "GoldenTotem"}

-- ==================== メインループ ====================
task.spawn(function()
    while true do
        task.wait(0.35)
        
        pcall(function()
            -- Auto Buy Meat
            if AutoBuyMeat() then
                for _, meat in ipairs(MEATS) do
                    local buyRemote = rs:FindFirstChild("BuyMeat") or rs.Events:FindFirstChild("BuyMeat") or rs:FindFirstChild("Purchase")
                    if buyRemote then
                        buyRemote:FireServer(meat)
                    end
                    task.wait(0.5)
                end
            end
            
            -- Auto Buy Gear
            if AutoBuyGear() then
                for _, gear in ipairs(GEARS) do
                    local gearRemote = rs:FindFirstChild("BuyGear") or rs.Events:FindFirstChild("BuyGear") or rs:FindFirstChild("Purchase")
                    if gearRemote then
                        gearRemote:FireServer(gear)
                    end
                    task.wait(0.6)
                end
            end
            
            -- Auto Cook
            if AutoCook() then
                local cookRemote = rs:FindFirstChild("Cook") or rs.Events:FindFirstChild("Cook") or rs:FindFirstChild("GrillEvent")
                for _, grill in ipairs(ws:FindFirstChild("Grills") and ws.Grills:GetChildren() or {}) do
                    if cookRemote then
                        cookRemote:FireServer(grill)
                    end
                end
            end
            
            -- Auto Discard Burnt
            if AutoDiscard() then
                local discardRemote = rs:FindFirstChild("Discard") or rs.Events:FindFirstChild("Discard") or rs:FindFirstChild("Trash")
                for _, item in ipairs(player.Backpack:GetChildren()) do
                    local n = item.Name:lower()
                    if n:find("burnt") or n:find("bad") or n:
